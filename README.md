# Editable Contracts API

REST API endpoints for editable-contracts <https://github.com/bclarkau/editable-contracts>

### Install Dependencies

`yarn install`

### Run

`node index` to start local server at localhost:8080